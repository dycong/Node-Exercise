/**
 * 包描述文件
 *    name 包名
 *    version 版本
 *    dependencies 生产依赖：项目运行时需要的依赖
 *    devdependencies 开发依赖：项目打包时需要的依赖
 *    scripts 启动包的指令
 */

const $ = require('jquery');
const fs = require('fs');
console.log(fs);
console.log($);